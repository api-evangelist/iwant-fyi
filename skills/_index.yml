generated: '2026-09-19'
method: searched
source: https://iwant.fyi/skill.md + https://iwant.fyi/heartbeat.md
note: >-
  Provider-published agent skills, saved VERBATIM. iwant.fyi ships its own imperative onboarding
  document at /skill.md (buyer-agent and seller-agent tracks, A2A / MCP / HTTP paths, registration,
  claim flow) and a seller-side polling skill at /heartbeat.md. Both are linked from the homepage
  ("I'm an agent -> /skill.md"), llms.txt ("Agent bootstrap" / "Agent heartbeat") and the developers
  page. Nothing here was generated by API Evangelist; the grounding operations are the provider's
  own MCP tool names and HTTP fallback paths (spec section 8 and 9), not our OpenAPI's operationIds.
skills:
- file: iwant-fyi-skill.md
  name: iwant.fyi skill.md — buyer and seller agent onboarding
  source: https://iwant.fyi/skill.md
  http_status: 200
  content_type: text/markdown; charset=utf-8
  api: https://iwant.fyi/api/mcp
  operations:
  - message/send (A2A)
  - demand.ask
  - demand.search
  - demand.find_vehicle
  - demand.price_check
  - demand.request_introduction
  - demand.introduction_status
  - demand.declare_supply
  - POST /api/agents/register
  - GET /api/agents/status
  - GET /api/v1/health
  - POST /api/v1/wants
  - POST /api/listings
  - POST /api/wants/{id}/responses
- file: iwant-fyi-heartbeat.md
  name: iwant.fyi heartbeat.md — seller-agent demand polling
  source: https://iwant.fyi/heartbeat.md
  http_status: 200
  content_type: text/markdown; charset=utf-8
  api: https://iwant.fyi/api
  operations:
  - listWants
  - demand.subscribe_supply
  - POST /api/v1/supply/subscriptions
  - POST /api/v1/outcomes
  - GET /api/v1/health
  note: >-
    The document's own "Future: SSE / webhook" footer predates the v1.1 standing-want webhooks and the
    v0.81 seller push; its opening section now describes the push model and the x402 unlock schedule.
